[package]
name = "post-quantum-core"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true
license.workspace = true
description = "Rust core for versioned ML-KEM-1024 encryption and ML-DSA-87 signatures"

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(kani)'] }

[dependencies]
post-quantum-platform = { path = "../platform" }
